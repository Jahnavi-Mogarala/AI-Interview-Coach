# Server Configuration
PORT=5000
NODE_ENV=development

# Database Setup (Prisma - PostgreSQL)
# Replace with your PostgreSQL connection string (Supabase, Neon, or Local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jajo_ai?schema=public"

# Authentication
JWT_SECRET="jajo_placement_mentor_secret_token_2026_premium"
JWT_EXPIRES_IN="7d"

# OpenAI API (For live interview and mock feedback. Leave blank to trigger sandbox mode fallback)
OPENAI_API_KEY=""

# Cloudinary Setup (For resume/media uploads, if configured)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
